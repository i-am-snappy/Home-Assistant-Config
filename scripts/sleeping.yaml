---
sleeping:
  alias: Sleeping
  sequence:
  - service: input_boolean.turn_on
    data:
      entity_id: input_boolean.mode_sleeping
  - delay:
      minutes: 60
  - service: input_boolean.turn_off
    data:
      entity_id: input_boolean.mode_bedtime
